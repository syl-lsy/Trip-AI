---
description: 代码审查 — 安全、性能、可维护性只读审查
---

启动代码审查。由 reviewer Agent 执行只读审查，涵盖：

1. **安全性** — PII 泄漏、SQL 注入、JWT 校验、输入验证
2. **性能** — N+1 查询、懒加载、数据库索引、LLM 缓存
3. **可维护性** — 编码约定、重复逻辑、错误处理一致、配置化
4. **测试** — 代码路径覆盖、测试有意义、边界情况

**用法**：`/review-code`（默认审查当前变更文件）
